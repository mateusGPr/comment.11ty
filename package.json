{
  "name": "comment.11ty",
  "version": "1.0.0",
  "description": "CommentAbout frontend",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve & npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/styles/style.css --watch",
    "build": "ELEVENTY_PRODUCTION=true eleventy && NODE_ENV=production npx tailwindcss -i styles/tailwind.css -c styles/tailwind.config.js -o _site/styles/style.css --minify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "html-minifier": "^4.0.0",
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "tiny-editor": "^0.3.0"
  }
}
